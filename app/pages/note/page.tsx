import React from "react";

type Props = {};

const Note = (props: Props) => {
  return(
  <div className="flex justify-center items-center h-screen w-full bg-gray-200 ">
    <p>Edit notes / add notes</p>
  </div>);
};
export default Note;
